# RoyalBit Asimov Roadmap
#
# WHAT to build - milestones only
#
# Migration: Node.js/Puppeteer → Rust/chromiumoxide
# Goal: Single static binary, no node_modules, UPX compressed
#
# See: docs/PROTOCOL_GOALS.md for core values
# See: CHANGELOG.md for release history
# See: docs/adr/ for detailed rationale

current:
  version: "1.1.0"
  status: complete
  summary: "Rebrand & Release Infrastructure"
  goal: "REBRAND_RELEASE"
  deliverables:
    - "[x] Rebrand: ref-tools → ref (CLI) / RoyalBit Ref (product)"
    - "[x] Cargo.toml: crates.io publishing metadata"
    - "[x] GitHub Actions: CI workflow (test, lint, build)"
    - "[x] GitHub Actions: Release workflow (multi-arch)"
    - "[x] Targets: linux-x64, linux-arm64, macos-x64, macos-arm64, windows-x64"
    - "[x] update command: self-update from GitHub releases"
    - "[x] Update LICENSE, README, schemas for new branding"

next:
  - version: "1.2.0"
    summary: "Enhanced extraction"
    goal: "BETTER_EXTRACTION"
    deliverables:
      - "[ ] PDF table extraction"
      - "[ ] PDF heading detection improvements"
      - "[ ] Stale reference detection (not cited anywhere)"

backlog:
  - "MCP server mode (like forge-mcp)"
  - "Proxy support (--proxy)"
  - "Export to markdown report"
  - "Image/OCR extraction from PDFs"
