# RoyalBit Asimov Roadmap
#
# WHAT to build - milestones only
#
# Migration: Node.js/Puppeteer → Rust/chromiumoxide
# Goal: Single static binary, no node_modules, UPX compressed
#
# See: docs/PROTOCOL_GOALS.md for core values
# See: CHANGELOG.md for release history
# See: docs/adr/ for detailed rationale

current:
  version: "0.8.0"
  status: complete
  summary: "LLM-optimized output + deployment"
  goal: "LLM_READY"
  deliverables:
    - "[x] v0.6.0: verify-refs command with references.yaml schema"
    - "[x] v0.6.0: Status detection (ok/dead/redirect/paywall/login)"
    - "[x] v0.6.0: JSON Schema v1 (schemas/references.v1.schema.json)"
    - "[x] v0.7.0: fetch - structured sections[], links[], code[] output"
    - "[x] v0.7.0: Content cleaning (strip nav/footer/aside)"
    - "[x] v0.7.0: --raw and --cookies flags"
    - "[x] v0.7.1: check-links - compact JSON output"
    - "[x] v0.7.1: refresh-data - compact JSON output, removed --filter"
    - "[x] v0.8.0: deploy-kveldulf target (remote build)"
    - "[x] v0.8.0: Simplified Makefile"

next:
  - version: "0.9.0"
    summary: "PDF extraction"
    goal: "PDF_SUPPORT"
    deliverables:
      - "[ ] PDF to structured text (local extraction, no APIs)"
      - "[ ] Extract text, headings, tables from PDF"
      - "[ ] Output same JSON structure as fetch"

backlog:
  - "MCP server mode (like forge-mcp)"
  - "Proxy support (--proxy)"
