# RoyalBit Asimov Roadmap
#
# WHAT to build - milestones only
#
# Migration: Node.js/Puppeteer → Rust/chromiumoxide
# Goal: Single static binary, no node_modules, UPX compressed
#
# See: docs/PROTOCOL_GOALS.md for core values
# See: CHANGELOG.md for release history
# See: docs/adr/ for detailed rationale

current:
  version: "0.5.0"
  status: complete
  summary: "Rust migration complete"
  goal: "SHIP"
  deliverables:
    - "[x] Cargo.toml with dependencies (clap, chromiumoxide, tokio, serde)"
    - "[x] Makefile (build-static, build-compressed, cross-compile)"
    - "[x] src/main.rs with CLI skeleton (clap derive)"
    - "[x] check-links command with chromiumoxide"
    - "[x] refresh-data command with extractors"
    - "[x] E2E test suite (10 tests passing)"
    - "[x] README.md updated for Rust version"
    - "[x] Node.js code removed"
    - "[x] Binary size: 4.4MB (macOS, no UPX)"

next:
  - version: "0.6.0"
    summary: "Cross-compilation"
    goal: "DISTRIBUTE"
    deliverables:
      - "[ ] Linux x86_64 musl static binary"
      - "[ ] Linux aarch64 musl static binary"
      - "[ ] UPX compression on Linux (<2MB)"

backlog:
  - "MCP server mode (like forge-mcp)"
  - "Watch mode for continuous checking"
  - "HTML report output (--format html)"
  - "Proxy support (--proxy)"
  - "Cookie/auth support for paywalled sites"
